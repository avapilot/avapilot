# USDC on Avalanche — MCP Server

Auto-generated by [AvaPilot](https://github.com/avapilot/avapilot).

## Contract Info

| | |
|---|---|
| **Address** | `0xB97EF9Ef8734C71904D8002F8b6Bc66Dd9c48a6E` |
| **Chain** | Avalanche C-Chain |
| **Type** | Custom contract |
| **Verified** | ✅ Yes |

## Tools

**2 read tools** (query on-chain state):
- `admin` — () → address
- `implementation` — () → address

**3 write tools** (build unsigned transactions):
- `changeAdmin` — (newAdmin: address) → 
- `upgradeTo` — (newImplementation: address) → 
- `upgradeToAndCall` — (newImplementation: address, data: bytes) →  💰 payable

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Run the MCP server
python server.py
```

## Connect to Claude Desktop

Add to `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "usdc": {
      "command": "python",
      "args": ["/Users/jz/Coding/AvaPilot/avapilot/examples/usdc-avax/server.py"],
      "env": {
        "RPC_URL": "https://api.avax.network/ext/bc/C/rpc"
      }
    }
  }
}
```

## Connect to OpenClaw

Add the MCP server config to your OpenClaw setup to let your agent interact with this contract.

---

*Generated by AvaPilot v0.1.0*
